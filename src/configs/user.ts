import type { UserData } from "~/types";
import constants from "./Constants";

const user: UserData = {
  name: constants.name,
  avatar: "img/ui/avatar.jpeg",
  password: ""
};

export default user;
